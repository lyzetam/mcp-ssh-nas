# SSH NAS Configuration
# Copy to .env and fill in your values

# NAS hostname or IP address (required)
NAS_HOST=your_nas_host_here

# SSH port (optional, default: 22)
NAS_PORT=22

# SSH username (required)
NAS_USER=your_username_here

# SSH password (required)
NAS_PASSWORD=your_password_here
